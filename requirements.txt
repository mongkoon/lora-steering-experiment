# Versions observed in the original Google Colab experiment (2026-09-23).
# Google Colab GPU runtime provides PyTorch and CUDA; do not force reinstall torch.
# The original run reported torch 2.11.0+cu128 on an NVIDIA Tesla T4.
# Remove preinstalled torchao 0.10.0 if it causes a PEFT import error.
transformers==5.17.0
peft==0.21.0
accelerate==1.15.0
datasets==5.0.1
safetensors==0.8.0
